define("amber-rubx/Rubx-Tests", ["amber/boot", "amber_core/SUnit"], function($boot){
var $core=$boot.api,nil=$boot.nil,$recv=$boot.asReceiver,$globals=$boot.globals;
$core.addPackage('Rubx-Tests');
$core.packages["Rubx-Tests"].transport = {"type":"amd","amdNamespace":"amber-rubx"};

$core.addClass('RubxTest', $globals.TestCase, [], 'Rubx-Tests');

});
